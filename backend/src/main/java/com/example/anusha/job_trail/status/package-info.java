/**
 * Status pipeline and the append-only status event log that backs it.
 * Nothing here is ever updated in place — every transition is a new row,
 * which is what makes the subway-map view and analytics possible.
 */
package com.example.anusha.job_trail.status;
