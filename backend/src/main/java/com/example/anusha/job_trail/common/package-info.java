/**
 * Cross-cutting concerns shared by every feature package: global error
 * handling and app-wide configuration (OpenAPI, security bootstrap, etc.).
 */
package com.example.anusha.job_trail.common;
